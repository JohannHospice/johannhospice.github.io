mixin list(values, names)
    each value, i in values
        li(class={'uk-active': (current.path[0] == "index" && value == "/") || current.path[0] == value.substr(1)})
            a(href=value)=names[i]